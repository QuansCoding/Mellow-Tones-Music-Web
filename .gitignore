# Secrets
.env
frontend/.env

# Uploaded audio — user data, not source.
# NB: these were previously written with backslashes ("backend\uploads"), which
# git does not treat as a path separator, so nothing was actually ignored.
backend/uploads/

# Python
__pycache__/
*.py[cod]
venv/
.venv/

# Node
node_modules/
frontend/dist/

# Editor / OS
.DS_Store

# Claude Code tooling config — local convenience, not part of the project.
.claude/
