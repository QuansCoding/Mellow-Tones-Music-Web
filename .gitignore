# Secrets
.env
frontend/.env
backend/.env

# Uploaded audio — user data, not source.
# NB: these were previously written with backslashes ("backend\uploads"), which
# git does not treat as a path separator, so nothing was actually ignored.
backend/uploads/

# Python
__pycache__/
*.py[cod]
venv/
.venv/
dist/

# Node
node_modules/
frontend/dist/

# Editor / OS
.DS_Store